machine:
  timezone:
    Asia/Tokyo

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - scp -r ${DEPLOY_YEAR} ${SSH_USER}@${HOST_NAME}:${DEPLOY_DIR}/${DEPLOY_YEAR}